import { TopHolderPipe } from './top-holder.pipe';

describe('TopHolderPipe', () => {
  it('create an instance', () => {
    const pipe = new TopHolderPipe();
    expect(pipe).toBeTruthy();
  });
});
