import { ColorOptimizePipe } from './color-optimize.pipe';

describe('ColorOptimizePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorOptimizePipe();
    expect(pipe).toBeTruthy();
  });
});
